language: julia

os:
  - linux
  - osx
  - windows

julia:
  - 1.3
  - 1.4
  - nightly

jobs:
  allow_failures:
    - julia: nightly
    - os: windows
  fast_finish: true

notifications:
  email: false

codecov: true
coveralls: true
